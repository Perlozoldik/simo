# Shared environment variables (copy to .env at the repo root for docker-compose)

# Postgres
POSTGRES_USER=indrive
POSTGRES_PASSWORD=indrive_dev_password
POSTGRES_DB=indrive

# Redis
REDIS_PASSWORD=indrive_redis_password

# API
API_PORT=4000
API_URL=http://localhost:4000

# Web
WEB_PORT=3000
WEB_URL=http://localhost:3000
